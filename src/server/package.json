{
  "name": "@mhl/server",
  "version": "0.0.1",
  "description": "API server for My Health Logs",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "preinstall": "node -e \"if(process.version.slice(1).split('.')[0] < 24) { console.warn('\\x1b[33m⚠️  Warning: Expected Node 24+, found ' + process.version + '. Run nvm use\\x1b[0m') }\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsx watch src/server.ts",
    "lint": "eslint ."
  },
  "engines": {
    "node": ">=24.12.0"
  },
  "keywords": [],
  "author": "Ayush Pahwa",
  "license": "ISC",
  "packageManager": "pnpm@10.13.1",
  "dependencies": {
    "@fastify/swagger": "^9.6.1",
    "@fastify/swagger-ui": "^5.2.3",
    "fastify": "^5.6.2",
    "fastify-plugin": "^5.1.0",
    "fastify-type-provider-zod": "^6.1.0",
    "zod": "^4.1.13"
  }
}
